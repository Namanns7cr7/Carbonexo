steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '${_IMAGE}', '-f', 'deploy/backend.Dockerfile', '.']
images: ['${_IMAGE}']
options:
  machineType: 'E2_HIGHCPU_8'
timeout: '1200s'
